<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bitvilltecnologies.savicom">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".HOMEFUNCTION.AddLost"></activity>
        <activity android:name=".HOMEFUNCTION.LostItem" />
        <activity android:name=".AUTH.SignupActivity" />
        <activity android:name=".HOMEFUNCTION.Wanted" />
        <activity android:name=".AUTH.Profile" />
        <activity android:name=".Feed" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".AUTH.LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>